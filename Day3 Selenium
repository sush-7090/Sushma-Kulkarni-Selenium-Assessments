------Task 1------------
package Frame;

import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.edge.EdgeDriver;
import org.openqa.selenium.edge.EdgeOptions;

public class Assessment6 {

	public static void main(String[] args) {
		EdgeOptions option=new EdgeOptions();
		option.addArguments("--disable-notifications");
		
		WebDriver driver = new EdgeDriver();

        driver.manage().window().maximize();

        driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(20));
        driver.get("https://www.easemytrip.com/");
        
        driver.findElement(By.id("FromSector_show")).click();
        driver.findElement(By.id("spn2")).click();
        driver.findElement(By.id("Editbox13_show")).sendKeys("Delhi");
        driver.findElement(By.xpath("(//li)[1]")).click();
        
        driver.findElement(By.id("ddate")).click();
        driver.findElement(By.xpath("//li[@id='snd_6_12/09/2026']")).click();
        
        driver.findElement(By.id("myFunction4")).click();
        driver.findElement(By.id("add")).click();
        
        driver.findElement(By.id("traveLer")).click();
        
        driver.findElement(By.id("divSearchFlight")).click();
        
        System.out.println(driver.getCurrentUrl());
        
        if(driver.getCurrentUrl().equals("https://www.easemytrip.com/flight-search/listing?srch=BLR-Bangalore-India%7CBOM-Mumbai-India%7C12%2F09%2F2026&px=2-0-0&cbn=0&ar=undefined&isow=true&isdm=true&lang=en-us&IsDoubleSeat=false&CCODE=IN&curr=INR&apptype=B2C\r\n"
        		+ ""))
        {
        	System.out.println(" flight-results page is displayed");
        }
        else {
        	System.out.println(" flight-results page not displayed");
        }
	}
}

--------Task2------------------------
package Frame;

import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.edge.EdgeDriver;

public class Assessment7 {

	public static void main(String[] args) {
		WebDriver driver = new EdgeDriver();

        driver.manage().window().maximize();

        driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(20));
        driver.get("https://demoapps.qspiders.com/ui/datePick?sublist=0");
        
        WebElement cal = driver.findElement(By.xpath("//input[@placeholder='Select A Date']"));
        cal.click();
        
        WebElement calender = driver.findElement(By.cssSelector("[aria-label='Next Month']"));
        calender.click();
        
        WebElement select = driver.findElement(By.cssSelector("[aria-label='Choose Monday, October 12th, 2026']"));
        
        select.click();
        
        String selectedDate = cal.getAttribute("Value");

        System.out.println("Selected Date: " + selectedDate);

        if (selectedDate.contains("12")) {
            System.out.println("displayed correctly in the date field");
        } else {
            System.out.println("Not displayed correctly in the date field");
        }
        driver.quit();
	}

}

